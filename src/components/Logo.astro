---
interface Props {
  variant?: "light" | "dark";
}

const { variant = "dark" } = Astro.props;
const textClass = variant === "light" ? "text-white" : "text-[var(--text)]";
---

<a href="#top" class={`inline-flex items-center gap-3 ${textClass}`} aria-label="ComputerForms inicio">
  <span class="brand-slot grid h-14 w-56 place-items-center text-xs font-bold text-[var(--muted)]">
    <img class="hidden h-11 max-w-full object-contain" src="/brand/logo.svg" alt="ComputerForms" onload="this.classList.remove('hidden'); this.parentElement.dataset.loaded = 'true'" />
    <span class="brand-placeholder">Subir logo.svg</span>
  </span>
</a>
